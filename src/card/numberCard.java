package card;

import java.awt.Color;

public class numberCard extends card{

	public int number;
	public numberCard(int number, String color) {
		super(color);
		this.number = number;
	}
}
